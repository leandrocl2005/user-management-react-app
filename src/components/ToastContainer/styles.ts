import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  right: 0;
  top: 0;
  padding: 0px 16px 0px 0px;
  overflow: hidden;
  margin-top: 16px;
  z-index: 90;
`;
